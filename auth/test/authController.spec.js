const expect = require('chai').expect;

// eslint-disable-next-line no-undef
describe('Sample', function() {
    it('true should be always equal to true', function() {
        expect(true).to.be.equal(true);
    });
});
